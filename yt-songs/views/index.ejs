<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Youtube Music</title>
</head>

<body>

    <main>
        <h1>Youtube Music</h1>
        <% for(const content of contents ){ %>
            <% if(content.eId){ %>
                <article>
                    <iframe loading="lazy" width="640" height="360"
                        src="https://www.youtube.com/embed<%= content.eId.slice(3) %>" frameborder="0">
                    </iframe>
                    <h3>
                        <%= content.name %>
                    </h3>
                </article>
                <% } %>
                    <% } %>

    </main>
</body>

</html>